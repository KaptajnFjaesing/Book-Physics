\documentclass[crop=false]{standalone}
\usepackage{tikz}
\usepackage{graphicx}
\usepackage{cfr-lm} 
\usepackage[active,tightpage]{preview}
\usepackage{xcolor}

% --- colors ---
\definecolor{darkroyalblue}{RGB}{0,20,50}
\definecolor{burgundy}{RGB}{128,0,32}      % burgundy red

\PreviewEnvironment{tikzpicture}

% --- book sizes ---
\newlength{\bookwidth}   \setlength{\bookwidth}{155mm}
\newlength{\bookheight}  \setlength{\bookheight}{220mm}
\newlength{\spinewidth}  \setlength{\spinewidth}{6.4mm}
\newlength{\bleed}       \setlength{\bleed}{5mm}

\newlength{\totalwidth}  \setlength{\totalwidth}{\dimexpr 2\bookwidth+\spinewidth+2\bleed\relax}
\newlength{\totalheight} \setlength{\totalheight}{\dimexpr \bookheight+2\bleed\relax}

\usepackage[
paperwidth=\the\totalwidth,
paperheight=\the\totalheight,
margin=0pt
]{geometry}

\pgfmathsetmacro{\frontcenterfactor}{((155 - 10)*0.5+5)/155}

\begin{document}
	\noindent
	\thispagestyle{empty}
	\begin{tikzpicture}
		
		% --- fill entire page with horizontal split (red top, blue bottom) ---
		\fill[burgundy] (0,0.5\totalheight) rectangle (\totalwidth,\totalheight);
		\fill[darkroyalblue] (0,0) rectangle (\totalwidth,0.5\totalheight);
		
		% optional bleed guide
		\draw[very thin,dashed] (0,0) rectangle (\totalwidth,\totalheight);
		
		% content area (excluding bleed) – we keep this scope for positioning
		\begin{scope}[xshift=\bleed,yshift=\bleed]
			
			% No need to fill individual panels because the whole page already has the split.
			% However, we keep the scope to place logo and text relative to the content area.
			
			% logo (still on the back cover, bottom left)
			\node[anchor=south east, inner sep=0pt]
			at (\bookwidth - 10mm, 10mm)
			{\includegraphics[width=40mm,height=20mm]{0220093121.png}};
			
		\end{scope}
		
		% --- front text (all in the red top half) ---
		\node[align=center,text width=\dimexpr\bookwidth-20mm\relax,
		font=\fontsize{26}{32}\selectfont]
		at (\dimexpr\bleed+\bookwidth+\spinewidth+\frontcenterfactor\bookwidth\relax,
		\dimexpr\bleed+0.58\bookheight\relax)
		{\textcolor{white}{INTRODUCTION\\ TO PHYSICS}};
		
		% --- subtitle moved DOWN to avoid colour split (now at 0.40, fully in blue) ---
		\node[align=center,text width=\dimexpr\bookwidth-20mm\relax,
		font=\fontsize{18}{20}\selectfont]
		at (\dimexpr\bleed+\bookwidth+\spinewidth+\frontcenterfactor\bookwidth\relax,
		\dimexpr\bleed+0.44\bookheight\relax)   % <-- changed from 0.48 to 0.40
		{\textcolor{white}{THEORY, METHODS AND \\APPLICATIONS}};
		
		% author name (still in blue, below subtitle)
		\node[align=center,text width=\dimexpr\bookwidth-20mm\relax,
		font=\fontsize{14}{20}\selectfont]
		at (\dimexpr\bleed+\bookwidth+\spinewidth+\frontcenterfactor\bookwidth\relax,
		\dimexpr\bleed+0.32\bookheight\relax)   % adjusted slightly lower for balance
		{\textcolor{white}{JONAS PETERSEN}};
		
		% --- spine text (unchanged) ---
		\node[anchor=center,rotate=-90,transform shape,font=\fontsize{10}{16}\selectfont]
		at (\dimexpr\bleed+\bookwidth+0.5\spinewidth\relax,
		\dimexpr\bleed+0.6\bookheight\relax)
		{\textcolor{white}{JONAS PETERSEN}};
		
		\node[anchor=center,rotate=-90,transform shape,font=\fontsize{10}{16}\selectfont]
		at (\dimexpr\bleed+\bookwidth+0.5\spinewidth\relax,
		\dimexpr\bleed+0.35\bookheight\relax)
		{\textcolor{white}{INTRODUCTION TO PHYSICS}};
		
	\end{tikzpicture}
\end{document}